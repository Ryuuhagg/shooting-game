#pragma once
struct ImagePath {
	const char* path;
	int w;
	int h;
};

extern ImagePath power;
extern ImagePath rapid;
extern ImagePath heal;
extern ImagePath addBullet;
extern ImagePath barrier;